let altura_cm = 174;

let altura_m = 1.74;

let peso_kg = 82.2;

let altura = Math.ceil(altura_m);

let peso = Math.round(peso_kg);

let maxJS = Number.MAX_VALUE;

let YesOrNo = Boolean((maxJS + 1) === maxJS)